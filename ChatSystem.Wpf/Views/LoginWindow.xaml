<Window x:Class="ChatSystem.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:ChatSystem.Wpf.Converters"
        Title="ChatSystem - 登录" Height="420" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F0F2F5">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBoolConverter x:Key="InverseBool"/>
    </Window.Resources>

    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="💬 ChatSystem" FontSize="28" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#0D6EFD"/>

            <TextBlock Text="用户名" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Padding="10" FontSize="14" Margin="0,0,0,12"/>

            <TextBlock Text="密码" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordBox" Padding="10" FontSize="14" Margin="0,0,0,8"
                         PasswordChanged="PasswordBox_PasswordChanged"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,8"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}"/>

            <Button Content="登 录" Style="{StaticResource PrimaryBtn}"
                    Command="{Binding LoginCommand}" IsEnabled="{Binding CanLogin}"
                    Visibility="{Binding IsLoggingIn, Converter={StaticResource InverseBool}}"/>

            <TextBlock Text="登录中..." HorizontalAlignment="Center" Margin="0,8,0,0"
                       Visibility="{Binding IsLoggingIn, Converter={StaticResource BoolToVis}}"
                       Foreground="#666"/>

            <TextBlock HorizontalAlignment="Center" Margin="0,16,0,0">
                <Hyperlink Command="{Binding GoRegisterCommand}">没有账号？立即注册</Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
